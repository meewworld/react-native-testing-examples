# Unit Tests

```
// Simple unit test of the add function
test('Function "add" correctly adds 2 numbers together', () => {
  const number = 4;
  const expectedNumber = 8;
  expect(add(number, number)).toBe(expectedNumber);
});
```
